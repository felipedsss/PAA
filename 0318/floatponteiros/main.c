/*creva um programa em linguagem C que solicita ao usuário 
que digite um valor float. Em seguida, o programa deve armazenar esse 
valor em uma variável do tipo float e atribuir o endereço dessa variável 
a um ponteiro do tipo float. 
pOSTEriormente, o programa deve adicionar 10 
ao valor armazenado na variável através do ponteiro e, 
   por fim, imprimir o novo valor da variável. 
   Certifique-se de explicar o funcionamento do programa 
   e como ele alcança o resultado final.



   */

#include <stdio.h>

   int main() {
	       
	       float entrada, *ponteiroFloat;
	           printf("Digite um valor float:");
		       // Lê o valor da entrada padrão e coloca na variável do tipo float
		       //     scanf("%f", &entrada);
		       //         // Atribui o endereço da variável tipo float para o ponteiro do tipo float
		       //             ponteiroFloat = &entrada;
		       //                 // Soma 10 ao conteúdo apontado pelo ponteiro do tipo float
		       //                     *ponteiroFloat = *ponteiroFloat + 10;
		       //                         // Imprime o conteúdo da variável do tipo float
		       //                             
		       //                                 printf(" O novo valor da variável é: %.2f", entrada);
		       //                                     return 0;
		       //                                     }
